<%= render partial: "categories/partials/nav", locals: { id: @category.id, category: @category } %>
<section>
  <div class="pop-up-box">
    <%= image_tag @video.main_pic.url("regular") %>
  </div>
  <div class="pop-up-box">
    <h3><%= @video.title %></h3>
    <p class="pop-up-content"><%= @video.video %></p>
  </div>
</section>


