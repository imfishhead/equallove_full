<%# 請小心這個檔案和 ckeditor/init.js 修改完後仍然有可能被 cached 而沒有生效 %>
<%# 如果修改完沒有反應請刪除 tmp/cache/assets 確保讀到的是最新版本 %>
<%# if Rails.configuration.action_controller.asset_host %>
  <%# 修正 CKEditor 在使用 S3 的情況下路徑錯誤 %>
  <%#window['CKEDITOR_BASEPATH'] = '/assets/ckeditor/'; %>
<%# end %>
<% if Rails.configuration.action_controller.asset_host %>
<%
  base_path = ''
  base_path << Rails.application.config.action_controller.asset_host
  base_path << Rails.application.config.assets.prefix
  base_path << '/ckeditor/'
%>
var CKEDITOR_BASEPATH = '<%= base_path %>';
<% end %>
